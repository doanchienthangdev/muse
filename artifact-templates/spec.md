---
type: muse-artifact
artifact_type: spec
source_session: <session-path>
thread_id: <thread-id or null>
persona: <persona-id>
created_at: <ISO 8601>
status: draft
---

# Spec: <feature name>

**Author**: <user name>
**Date**: <YYYY-MM-DD>
**Version**: 0.1-draft

---

## Overview

<1-2 paragraphs: what this feature is, who it serves, what problem it solves.>

## User stories

<Each user story: as a <role>, I want <capability>, so that <outcome>. Bullet list. Grounded in the session's problem framing.>

- As a <role>, I want <capability>, so that <outcome>.
- As a <role>, I want <capability>, so that <outcome>.
- As a <role>, I want <capability>, so that <outcome>.

## Functional requirements

<Specific, testable requirements. Each numbered and imperative. "The system SHALL X" or "When <trigger>, the system MUST <response>".>

### F1. <Requirement name>
The system SHALL <specific behavior>.

### F2. <Requirement name>
When <trigger>, the system MUST <response>.

### F3. <Requirement name>
The system SHALL NOT <forbidden behavior>.

<Add as many as the session surfaced.>

## Non-functional requirements

<Performance, reliability, security, accessibility, etc. Measurable. Not "fast" — "p99 latency < 200ms".>

### NF1. <Requirement name>
<specific measurable target>

## Data model

<Schema for any new data, or changes to existing data. Include fields, types, constraints, relationships.>

```
<schema sketch — can be SQL DDL, TypeScript interface, protobuf, YAML, whatever fits>
```

## API surface

<Any new endpoints, CLI flags, or public interfaces. Include request/response shapes.>

```
<api sketch>
```

## Edge cases

<Each edge case the session surfaced. Each gets a behavior spec.>

- <edge case 1>: system behavior → <specific>
- <edge case 2>: system behavior → <specific>
- <edge case 3>: system behavior → <specific>

## Error handling

<Every error state + what the user sees + whether it's logged + how it's recoverable.>

| Error | User sees | Logged? | Recovery |
|---|---|---|---|
| <error 1> | <message> | Y/N | <action> |
| <error 2> | <message> | Y/N | <action> |

## Testing plan

<What tests prove this works. Unit, integration, E2E. Mention golden files or fixtures if applicable.>

### Happy path tests
- <test 1>
- <test 2>

### Edge case tests
- <test 1>
- <test 2>

### Load / performance tests (if applicable)
- <test description>

## Out of scope

<Explicit list of things adjacent to this spec that are NOT in it. Prevents scope creep.>

## Open questions

<Anything the session didn't fully resolve.>

- <Q1>
- <Q2>

## Session source

- Session: `<session-path>`
- Thread: `<thread-id or "single-session">`
- Persona: <Full Name>
- Date: <YYYY-MM-DD>

---

*Generated by muse v2.16. This spec is buildable; the session log shows how we got here.*
